// Background worker entrypoint — polls due scraper sources and runs scrape jobs.
// Full implementation in Phase 3. For now this is a keep-alive stub.
console.log("[worker] DamgedCarsScanner background worker started");
console.log("[worker] Scrape loop will be implemented in Phase 3");

// Keep the process alive
setInterval(() => {}, 60_000);
